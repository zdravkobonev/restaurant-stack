apiVersion: v1
kind: Service
metadata:
  name: {{ include "restaurant.fullname" (dict "root" . "name" "backend") }}
  namespace: {{ include "restaurant.ns" . }}
  labels:
    {{- include "restaurant.labels" (dict "root" . "component" "backend") | nindent 4 }}
spec:
  selector:
    {{- include "restaurant.selectorLabels" (dict "root" . "component" "backend") | nindent 4 }}
  ports:
    - name: http
      port: 80
      targetPort: {{ .Values.backend.port }}
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "restaurant.fullname" (dict "root" . "name" "backend") }}
  namespace: {{ include "restaurant.ns" . }}
  labels:
    {{- include "restaurant.labels" (dict "root" . "component" "backend") | nindent 4 }}
spec:
  replicas: 1
  selector:
    matchLabels:
      {{- include "restaurant.selectorLabels" (dict "root" . "component" "backend") | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "restaurant.selectorLabels" (dict "root" . "component" "backend") | nindent 8 }}
    spec:
      containers:
        - name: backend
          image: "{{ .Values.images.backend.repository }}:{{ .Values.images.backend.tag }}"
          imagePullPolicy: {{ .Values.images.backend.pullPolicy }}
          ports:
            - containerPort: {{ .Values.backend.port }}
          envFrom:
            - secretRef:
                name: {{ include "restaurant.fullname" (dict "root" . "name" "app-env") }}
          env:
{{- with .Values.backend.env }}
{{- range . }}
            - name: {{ .name }}
              value: {{ .value | quote }}
{{- end }}
{{- end }}
            # --- CORS (nil-safe) ---
{{- $cors := default (dict) .Values.backend.cors }}
{{- $allowCreds := default true $cors.allowCredentials }}
{{- $allowMethods := default (list "GET" "POST" "PUT" "PATCH" "DELETE" "OPTIONS") $cors.allowMethods }}
{{- $allowHeaders := default (list "Authorization" "Content-Type") $cors.allowHeaders }}
            - name: CORS_ALLOW_ORIGINS
              value: {{ include "restaurant.cors.allowedOrigins" . | quote }}
            - name: CORS_ALLOW_CREDENTIALS
              value: {{ $allowCreds | quote }}
            - name: CORS_ALLOW_METHODS
              value: {{ join "," $allowMethods | quote }}
            - name: CORS_ALLOW_HEADERS
              value: {{ join "," $allowHeaders | quote }}
          readinessProbe:
            httpGet:
              path: /health
              port: {{ .Values.backend.port }}
            initialDelaySeconds: 5
          livenessProbe:
            httpGet:
              path: /health
              port: {{ .Values.backend.port }}
            initialDelaySeconds: 10
